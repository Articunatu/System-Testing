using System;

namespace CalculateSum
{
    class Program
    {
        static void Main()
        {
            Math math = new Math();
            int x = 8, y = 8;
            var sum = math.Sum(x, y);
        }
    }
}
